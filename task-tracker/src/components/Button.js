const Button = ({ color, text, toggleShow }) => {
    const handleClick = () => {
     
    };
  return (
        
     <div>
      <button onClick={handleClick} className="btn">
       Show Add Task Bar
      </button>
    </div>
  );
};

export default Button;